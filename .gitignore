# lingkungan
.venv/
__pycache__/
*.pyc

# bobot model (unduh sendiri)
*.pt

# kredensial (JANGAN commit)
.env
# config kamera runtime — jadwal = pola kapan penghuni pergi (privasi)
cameras.json

# footage & data runtime (privasi — video rumah & aktivitas keluarga)
out/
samples/
klip_*.mp4
snap_*.jpg
events-live.jsonl
events-out.out
episodes-live.jsonl
# DB bersama (event rumah/keluarga — privasi)
cctv.db
cctv.db-wal
cctv.db-shm
debug.on
debug-zona/out/
